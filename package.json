{
  "private": true,
  "workspaces": {
    "packages": ["products", "home"]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  },
  "keywords": [],
  "name": "webpack-module-federation",
  "description": "",
  "dependencies": {
    "lerna": "4.0.0"
  }
}
